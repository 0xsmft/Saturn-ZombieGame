API 0.1 - Assets
 -> Avoid using raw AssetID, should use SPROPERTY and AssetReferences

API 1.1 - General texture fixes
 -> Why should entities hold a texture and why do we have to create them in such way
 -> Currently, we have to create textures by creating their TextureSourceAsset (and we have to convert the path to an absolute path) then we create the real Texture2D

API 2.0 - Parenting fixes
 -> Calling SetParent() should automatically set the child's parent and add the child to the parents list
 -> Auto-remove child if parent is changed from one entity to another

API 3.0 - Alura
 -> Please note that Alura is still in early stages anyways so many things will change!
 -> General uneasy API to use!

API 3.1 - Alura, calculating text size twice
 -> When calling CalcTextSize for custom position for text elements, Alura then calls CalcTextSize again when AddText is called, not really ideal

API 3.2 - Alura, adding text
 -> Avoid using std::format, should add AluraCanvas::AddText(fmt,...)

API 4.0
 -> Unloading sounds, we shouldn't have to call AudioSystem::UnloadSound()

